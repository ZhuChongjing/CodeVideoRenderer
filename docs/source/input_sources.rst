输入源处理逻辑
==============================

库内部会对输入的代码进行严格的预处理，以保证渲染稳定性：

1. Tab 转换

   所有的制表符 ``\t`` 会被自动展开为空格（默认宽度 :data:`~.DEFAULT_TAB_WIDTH`）。

2. 字符校验

   自动检测代码中是否包含 :math:`\mathbb{M}\text{anim}` 无法渲染的非法字符。

3. 空行处理

   代码首尾的空行会被自动剔除，但保留段落间的空行以维持格式。
